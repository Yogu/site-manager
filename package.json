{
	"name": "site-manager",
	"version": "0.0.1",
	"description": "Upgrade, backup and manage multiple instances of the same website",
	"main": "index.js",
	"author": "Jan Melcher <info@yogularm.de>",
	"license": "GPLv2",
	"dependencies": {
		"socket.io": "0.9.x",
		"express": "3.5.x",
		"es6-promise": "*",
		"js-yaml": "3.0.x",
		"bower": "1.3.x",
		"express-promise": "0.1.x"
	},
	"devDependencies": {
		"coveralls": "^2.10.0",
		"jasmine-node": "1.14.x",
		"rimraf": "2.2.x",
		"ncp": "0.5.x",
		"walk": "2.2.x",
		"karma": "~0.10",
		"protractor": "~0.20.1",
		"phantomjs": "1.9.x"
	},
	
	"scripts": {
		"postinstall": "bower install",
		
		"test": "npm run server-unit-test && npm run client-unit-test && npm run end-to-end-test",
		"server-unit-test": "jasmine-node --captureExceptions test/server-unit",
		"client-unit-test": "karma start test/karma-conf.js",
		"end-to-end-test": "webdriver-manager update && protractor test/protractor-conf.js"
	}
}
